<% provide(:title, 'Games') %>
<div class="row">
	<h1>All Completed Games</h1>
	
	<aside class="span3">
		<section>
      <%= render 'shared/user_info' %>
    </section>
    <section>
      <%= render 'shared/stats' %>
    </section>
  </aside>

  <div class="span9">
  	<%= will_paginate %>

		<ul class="users">
			<%= render @games || "There are no completed games on record." %>
		</ul>

		<%= will_paginate %>
		
  </div>
</div>